import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    backgroundColor: "#fff",
    height: "100%",
    width: "100%",
    position: "absolute",
    borderRadius: 20,
    zIndex: 10,
    elevation: 5,
  },
});

export default styles;
